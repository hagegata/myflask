## 2026-08-25: Настройка GitHub Actions

**Задача:** автоматизировать сборку Docker-образа при пуше в main.

**Выполнено:**
- Создан `.github/workflows/docker-build.yml`.
- Workflow собирает образ `myflask-web`.
- Проверено на GitHub: сборка успешна.

**Ошибки и решения:**
# Worklog

## 2026-08-19: Первые шаги в Docker

- Создано простое Python-приложение (`app.py`), печатающее строку.
- Написан первый `Dockerfile` с инструкциями `FROM`, `WORKDIR`, `COPY`, `CMD`.
- Собран образ `myapp` и запущен контейнер.

## 2026-08-20: Flask в Docker

- Создано Flask-приложение, отвечающее на HTTP-запросы.
- Добавлен `requirements.txt` с зависимостью `flask`.
- Собран образ `myflask` и запущен контейнер в режиме host-сети.

## 2026-08-20: Переменные окружения

- Приложение научилось читать `WELCOME_MSG` из переменной окружения.
- Проверена передача переменной при запуске контейнера через `-e`.

## 2026-08-24: Healthcheck и ограничения ресурсов

- В Dockerfile добавлен `HEALTHCHECK` для проверки доступности приложения.
- Контейнер запущен с лимитами памяти (`--memory=128m`) и CPU (`--cpus=0.5`).
- Проверено, что статус контейнера становится `(healthy)`.

## 2026-08-24: Тома (Volumes)

- Создан том `mydata`.
- Данные записаны в том из одного контейнера и прочитаны из другого.
- Подтверждено, что том сохраняет данные между запусками контейнеров.

## 2026-08-24: Docker Compose (один сервис)

- Написан `docker-compose.yml` для сервиса `web`.
- Сервис запускается через `docker-compose up -d`.
- Проверена работа приложения через `curl`.

## 2026-08-24: Docker Compose с Redis

- Добавлен сервис `redis` в `docker-compose.yml`.
- Приложение Flask подключено к Redis и хранит счётчик посещений.
- Проверено, что счётчик увеличивается при каждом запросе и сохраняется при перезапуске.

## 2026-08-25: Настройка Git и GitHub

- Инициализирован локальный Git-репозиторий.
- Сделан первый коммит.
- Настроена SSH-аутентификация для GitHub.
- Проект запушен в удалённый репозиторий `hagegata/myflask`.

## 2026-08-25: GitHub Actions (CI)

- Создан workflow `.github/workflows/docker-build.yml`.
- Настроена автоматическая сборка Docker-образа при пуше в `main`.
- Проверен успешный запуск workflow на GitHub.

## 2026-08-26: Многостадийная сборка Dockerfile

- Dockerfile переведён на multi-stage build.
- Зависимости устанавливаются в стадии `builder` и копируются в финальный образ.
- Уменьшен размер итогового образа и убраны лишние инструменты.

## 2026-08-26: Deployment и Service в Kubernetes

- Создан манифест Deployment для Nginx (2 реплики).
- Создан Service типа ClusterIP.
- Объекты созданы, поды не стартуют из-за отсутствия CNI (ожидаемо на текущем кластере).


## 2026-08-27: Настройка CI/CD пайплайна

- Создан `docker-compose.ci.yml` для тестового запуска.
- В GitHub Actions добавлены шаги: сборка, запуск через Compose, тест HTTP-запроса, пуш в GHCR.
- Настроены разрешения `packages: write`.
- Пайплайн успешно выполняется на GitHub.
